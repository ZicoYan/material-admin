import React from 'react';

/**
 * 订单列表，
 * 筛选 - 支持按照厂家id、厂家名称、厂家类型、日期筛选
 * 排序 - 按照日期排序
 */

interface IEmployeeListProps {}

const EmployeeList: React.FC<IEmployeeListProps> = props => {
  return <>Hi, I'm Order list</>;
};

export default EmployeeList;
